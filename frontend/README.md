# Frontend (Next.js)

La aplicación web vive en esta carpeta. El código fuente está en `src/` (App Router).

Desde la raíz del monorepo:

```bash
npm run dev:web
# o
npm run dev --workspace=frontend
```

Variables: `NEXT_PUBLIC_API_URL=http://localhost:4000/api/v1`

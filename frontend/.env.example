# Frontend Next.js — copiar a frontend/.env.local (NO subir a Git)
# Producción: configurar en Vercel → Environment Variables

# Local
NEXT_PUBLIC_API_URL=http://localhost:4000/api/v1

# Producción (Vercel) — URL pública del backend en Railway, con /api/v1
# NEXT_PUBLIC_API_URL=https://tu-backend.up.railway.app/api/v1

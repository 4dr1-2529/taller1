# Dataset real

Esta carpeta no contiene datos institucionales. El entrenamiento científico requiere `DATASET_PATH` apuntando a un CSV autorizado y anonimizado.

El CSV debe incluir las columnas de `FEATURE_NAMES` y `target`. No se generan etiquetas ni datos sintéticos cuando `ML_DATA_MODE=real` (valor predeterminado).